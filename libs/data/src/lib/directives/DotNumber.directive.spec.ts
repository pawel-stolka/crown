import { DotNumberDirective } from './DotNumber.directive';

describe('DotNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new DotNumberDirective();
    expect(directive).toBeTruthy();
  });
});
