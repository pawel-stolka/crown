import { SumByMonthPipe } from './sumByMonth.pipe';

describe('SumByMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new SumByMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
