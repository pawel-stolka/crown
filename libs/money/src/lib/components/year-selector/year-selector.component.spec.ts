import { YearSelectorComponent } from './year-selector.component';

describe('YearSelectorComponent', () => {
  let component: YearSelectorComponent;

  beforeEach(() => {
    component = new YearSelectorComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
