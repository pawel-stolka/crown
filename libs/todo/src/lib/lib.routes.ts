import { Route } from '@angular/router';
import { TodoContainerComponent } from './container/todo-container.component';

export const todoRoutes: Route[] = [{ path: '', component: TodoContainerComponent }];
